# 프로젝트 구조 결함 검토

대상 프로젝트: `haji-charge-route`

검토 관점:

- 현재 서비스 기능에 맞는 구조인지
- 기능별 책임이 적절히 분리되어 있는지
- 불필요한 로직, 데모 정보, 개인 정보성 데이터가 섞여 있는지
- 개발/테스트 앱으로 유지할 때 먼저 정리해야 할 구조 결함이 무엇인지

---

## 1. 전체 판단

현재 구조는 큰 방향에서는 적당하다.

```text
frontend
  Next.js 화면, 카카오 지도, 세션 저장, API 호출

backend
  FastAPI 라우터, 외부 API 연동, 충전 계획 계산

docs
  충전 경로 최적화 검토 문서
```

서비스 기능도 다음 흐름으로 자연스럽게 나뉘어 있다.

```text
출발지/도착지/현충전량 입력
→ 장소 검색
→ 경로 조회
→ 경로 주변 충전소 조회
→ 차량 기준 충전 계획 계산
→ 지도/상세/충전 계산 화면 표시
```

다만 기능 검증을 빠르게 하기 위해 만든 흔적들이 서비스 구조 안에 그대로 섞여 있다. 개인 테스트 앱으로는 동작하지만, 계속 개발하면 다음 문제가 커진다.

- 데모 데이터와 실데이터가 같은 서비스 함수 안에 섞여 있다.
- 개인 주소, 브랜드 로고, mock 충전소가 실제 기능 코드에 포함되어 있다.
- 백엔드와 프론트가 같은 충전 개념을 서로 다른 방식으로 계산한다.
- UI 페이지가 화면, 상태 복원, API 호출, 계산 결과 표시를 한 파일에서 모두 처리한다.
- 일부 문서와 코드의 핵심 수치가 불일치한다.
- 생성 산출물과 로컬 환경 폴더가 프로젝트 안에 남아 있다.

---

## 2. 현재 프로젝트 구조

현재 주요 구조는 다음과 같다.

```text
haji-charge-route/
├─ backend/
│  ├─ app/
│  │  ├─ main.py
│  │  ├─ config.py
│  │  ├─ models.py
│  │  ├─ routers/
│  │  │  ├─ route.py
│  │  │  ├─ places.py
│  │  │  ├─ stations.py
│  │  │  └─ weather.py
│  │  └─ services/
│  │     ├─ charging.py
│  │     ├─ ev_stations.py
│  │     ├─ kakao.py
│  │     └─ weather.py
│  ├─ .env
│  ├─ .env.example
│  ├─ .venv/
│  ├─ frontend/
│  └─ requirements.txt
├─ frontend/
│  ├─ src/
│  │  ├─ app/
│  │  │  ├─ login/
│  │  │  ├─ main/
│  │  │  ├─ map/
│  │  │  ├─ stations/[id]/
│  │  │  └─ charge/
│  │  ├─ components/
│  │  └─ lib/
│  ├─ public/
│  ├─ node_modules/
│  ├─ .next/
│  ├─ .env.local
│  └─ tsconfig.tsbuildinfo
├─ docs/
├─ README.md
├─ 보완사항.md
├─ bydLogo.png
└─ 프레젠테이션1.pptx
```

구조상 핵심 결함은 `backend/.venv`, `frontend/node_modules`, `frontend/.next`, `frontend/tsconfig.tsbuildinfo`, `backend/frontend`, 루트의 `bydLogo.png`, `프레젠테이션1.pptx`처럼 실행·빌드·기획 산출물과 앱 소스가 뒤섞여 있다는 점이다.

---

## 3. 구조상 적절한 부분

## 3.1 프론트/백엔드 분리

프론트가 직접 공공 API 키를 들고 있지 않고, 백엔드를 통해 경로와 충전소 데이터를 받는 구조는 적절하다.

적절한 이유:

- 카카오 REST API 키와 공공데이터 API 키를 브라우저에 노출하지 않는다.
- 충전 계획 계산을 백엔드에서 일관되게 수행할 수 있다.
- 나중에 캐시, rate limit, 인증을 백엔드에 붙이기 쉽다.

유지 권장:

- `frontend/src/lib/api.ts`는 백엔드 REST 호출만 담당한다.
- 외부 API 호출은 계속 `backend/app/services` 아래에 둔다.

## 3.2 백엔드 라우터와 서비스 계층 분리

현재 라우터와 서비스는 대체로 역할이 나뉘어 있다.

```text
routers/route.py      경로 계획 API 조립
routers/places.py     장소 검색 API
routers/stations.py   충전소 상세 API
routers/weather.py    날씨 API

services/kakao.py       카카오 API
services/ev_stations.py 공공 충전소 API
services/weather.py     날씨 API
services/charging.py    충전 계획 계산
```

큰 방향은 좋다. 다만 각 서비스 파일 안에 너무 많은 세부 책임이 몰려 있어 아래 개선이 필요하다.

---

## 4. 주요 구조 결함

## 4.1 `backend` 안에 `frontend` 폴더가 중첩되어 있음

위치:

- `backend/frontend/public`

문제:

- 루트에도 `frontend/`가 있는데 `backend/frontend/`가 별도로 존재한다.
- 현재 `backend/frontend` 안에는 `public` 폴더만 있어 실제 앱 구조와 맞지 않는다.
- 신규 개발자가 보면 백엔드 내부에 또 다른 프론트 프로젝트가 있는 것으로 오해할 수 있다.
- 배포 스크립트나 파일 검색, 복사 작업에서 혼동을 만든다.

판정:

- 불필요한 구조로 보인다.

권장:

- `backend/frontend`가 실제로 사용되지 않는다면 삭제한다.
- 프론트 정적 자산은 루트 `frontend/public`만 사용한다.

---

## 4.2 로컬 환경과 빌드 산출물이 프로젝트에 섞여 있음

위치:

- `backend/.venv`
- `frontend/node_modules`
- `frontend/.next`
- `frontend/tsconfig.tsbuildinfo`

문제:

- 프로젝트 탐색과 검색 결과가 크게 오염된다.
- 구조 분석, 보안 스캔, 파일 백업 시 불필요한 파일이 대량 포함된다.
- 특히 `backend/.venv`는 2,000개 이상의 파일을 포함하고 있어 저장소/백업 대상이 되면 부담이 크다.

현재 `.gitignore`와 `frontend/.gitignore`에는 제외 규칙이 있으므로 Git 추적은 피할 수 있지만, 프로젝트 폴더 자체는 계속 무거워진다.

권장:

- 로컬 개발 환경은 유지하되, 구조 문서와 배포 스크립트에서는 제외한다.
- 가능하면 `.venv`는 루트가 아니라 개발자 로컬 위치에 두거나, 현재 위치를 유지하되 백업/압축 대상에서 제외한다.
- `tsconfig.tsbuildinfo`는 이미 `frontend/.gitignore` 대상이므로 공유하지 않는다.

---

## 4.3 개인 주소가 UI 기능 코드에 박혀 있음

위치:

- `frontend/src/app/main/page.tsx`

관련 내용:

```text
슈야집
서울 마포구 대흥로9길 25

공듀집
목동신시가지3단지아파트 303동
```

문제:

- 개인 주소 또는 개인 식별 가능성이 있는 별칭이 앱 기능 코드에 포함되어 있다.
- 테스트 앱이라도 화면 캡처, GitHub 공개, 시연 시 노출 위험이 있다.
- 서비스 기능에는 필수 요소가 아니다.

판정:

- 서비스 기능 구조상 불필요한 정보다.
- 개인 테스트 편의 기능이라면 앱 코드와 분리해야 한다.

권장:

- 소스에서 제거한다.
- 필요하면 `.env.local` 또는 개인 설정 JSON으로 분리한다.
- 라벨은 `집`, `회사`, `자주 가는 곳 1`처럼 일반화한다.
- 개인 설정 파일은 Git 제외한다.

권장 구조:

```text
frontend/src/lib/personal-shortcuts.ts
  개발 시에만 mock 또는 env에서 읽음

frontend/src/app/main/page.tsx
  바로가기 UI만 렌더링
```

---

## 4.4 브랜드 로고와 BYD 공식성 오인 요소가 구조에 포함됨

위치:

- `bydLogo.png`
- `frontend/public/bydLogo.png`
- `frontend/src/app/login/page.tsx`
- `frontend/src/app/layout.tsx`
- `backend/app/services/charging.py`

문제:

- 앱 첫 화면에 BYD 로고가 크게 표시된다.
- 앱 제목과 설명이 BYD 공식 앱처럼 보일 수 있다.
- 개인 테스트 앱으로는 가능하더라도 공개 저장소나 시연물로 넘어가면 브랜드/상표 리스크가 생긴다.

판정:

- 차량 기준값으로 BYD 돌핀을 사용하는 것은 서비스 기능상 필요하다.
- 그러나 BYD 로고 파일과 공식 앱처럼 보이는 로그인 화면은 기능상 필수는 아니다.

권장:

- 루트 `bydLogo.png`는 제거하고 `frontend/public`에만 자산을 둔다.
- 공개 가능성이 있으면 BYD 로고 대신 자체 아이콘을 사용한다.
- 화면 문구는 `BYD 돌핀 기준 참고 계산` 정도로 낮춘다.
- `로그인` 버튼은 실제 인증이 없으므로 `시작하기`로 변경한다.

---

## 4.5 README와 코드의 차량 주행거리 기준 불일치

위치:

- `README.md`
- `backend/app/services/charging.py`
- `backend/app/models.py`
- `docs/route-optimization-review.md`

문제:

- README에는 BYD 돌핀 스탠다드 만충 주행거리가 `340km`라고 적혀 있다.
- 실제 계산 코드에서는 `range_km=300.0`이다.
- 모델 주석과 검토 문서는 `300km` 기준으로 보인다.

영향:

- 같은 입력값으로 설명과 실제 결과가 다르게 해석된다.
- 기능 검증 시 사용자가 "왜 충전소가 더 많이 잡히는지" 이해하기 어렵다.

판정:

- 구조 결함이라기보다 서비스 기준값 관리 결함이다.
- 하지만 충전 경로 앱에서는 핵심 도메인 값이므로 우선순위가 높다.

권장:

- 차량 스펙을 코드 상수에서 별도 설정 파일로 분리한다.
- README, 화면, API 응답이 같은 값을 보게 한다.

권장 구조:

```text
backend/app/domain/vehicles.py
  VehicleSpec 정의
  DOLPHIN_STANDARD

backend/app/services/charging.py
  차량 스펙을 입력받아 계산만 수행
```

---

## 4.6 mock 데이터와 실데이터 연동 로직이 같은 파일에 섞여 있음

위치:

- `backend/app/services/kakao.py`
- `backend/app/services/ev_stations.py`

문제:

- 카카오 키가 없으면 `_MOCK_PLACES`를 사용한다.
- 공공 API 실패 시 `_MOCK_STATIONS`, `_MOCK_DETAIL`로 폴백한다.
- mock 데이터가 실제 서비스 코드의 하단에 직접 들어 있다.
- 실데이터 호출 실패와 개발용 mock 사용이 구조적으로 구분되지 않는다.

영향:

- 외부 API가 실패했는데 사용자는 mock 결과를 실제 결과로 오해할 수 있다.
- 테스트 데이터가 점점 늘어나면 실제 서비스 코드가 무거워진다.
- 실주행 테스트 중 잘못된 충전소가 추천될 수 있다.

판정:

- 개인 테스트 편의상 mock은 유용하다.
- 하지만 현재 위치는 부적절하다.

권장:

```text
backend/app/services/kakao.py
  실 카카오 API 클라이언트

backend/app/services/ev_stations.py
  실 공공 API 클라이언트

backend/app/mocks/kakao.py
  장소 mock

backend/app/mocks/ev_stations.py
  충전소 mock

backend/app/config.py
  MOCK_ENABLED
```

추가 권장:

- `MOCK_ENABLED=false`일 때는 외부 API 실패 시 오류를 반환한다.
- 응답에는 항상 `data_source`를 명확히 포함한다.
- 프론트는 `data_source=mock`이면 화면에 눈에 띄게 표시한다.

---

## 4.7 `kakao.geocode()`의 서울 기본 폴백은 기능 결함

위치:

- `backend/app/services/kakao.py`

문제:

- 주소나 장소를 찾지 못하면 최종적으로 서울 좌표를 반환한다.
- 이 동작은 앱이 "에러 없이 돌아가게" 만들지만, 충전 경로 서비스에는 위험하다.

영향:

- 사용자가 잘못 입력한 주소가 서울로 계산될 수 있다.
- 출발지/도착지가 틀렸는데 충전소 추천이 정상처럼 보일 수 있다.

판정:

- 데모용으로는 편하지만, 서비스 기능에는 부적합하다.

권장:

- geocode 실패 시 예외를 발생시킨다.
- 라우터에서 400 오류와 사용자 친화 문구를 반환한다.
- mock 모드에서만 서울/부산 기본값을 허용한다.

---

## 4.8 경로 실패 시 직선거리 보간이 실제 경로처럼 사용됨

위치:

- `backend/app/services/kakao.py`

문제:

- 카카오 길찾기 실패 시 직선거리 x 1.3과 보간 좌표를 사용한다.
- 이 경로가 이후 충전소 투영, 충전 계획 계산, 지도 표시에 그대로 쓰인다.

영향:

- 실제 도로와 무관한 충전소가 추천될 수 있다.
- 고속도로 휴게소/IC 판단이 무의미해진다.

판정:

- 앱 데모에는 유용하지만 실제 기능으로는 위험하다.

권장:

- mock 모드가 아니면 경로 실패 시 계산을 중단한다.
- 추정 경로일 때는 `route_quality="estimated"` 같은 필드를 추가한다.
- 프론트에서 `추정 경로`를 명확히 표시한다.

---

## 4.9 충전 계산 로직이 백엔드와 프론트에 중복되어 있음

위치:

- `backend/app/services/charging.py`
- `frontend/src/lib/charge.ts`

문제:

- 백엔드 경로 계획은 충전 커브 기반 DP를 사용한다.
- 프론트 충전 계산 화면은 단순 정출력 계산을 사용한다.

```text
프론트 계산:
추가 충전량(kWh) / 충전기 출력(kW) * 60

백엔드 계산:
SoC별 충전 커브 + 충전기 출력 + 효율 + 정차 오버헤드
```

영향:

- 같은 충전소와 같은 목표 충전량인데 예상 시간이 다르게 보일 수 있다.
- 사용자 입장에서는 어느 계산이 맞는지 혼란스럽다.

판정:

- 중복 로직이다.
- 단순 계산 기능이 별도 목적이라면 명확히 이름을 바꿔야 한다.

권장:

- 충전 시간 계산은 백엔드 단일 API로 통합한다.
- 프론트는 입력 UI와 결과 표시만 담당한다.
- 단순 계산을 유지하려면 화면 이름을 `단순 충전량 계산`으로 바꾼다.

---

## 4.10 `charging.py`가 도메인 계산을 너무 많이 포함함

위치:

- `backend/app/services/charging.py`

현재 책임:

- 차량 스펙 정의
- 충전 커브
- 온도 보정
- 속도 보정
- 거리 계산
- 경로 샘플링
- 충전소 투영
- greedy 추천
- DP 최적화
- 휴게소/IC 패널티

문제:

- 한 파일이 490라인이고, 도메인 개념이 많다.
- 계산 검증, 차량 스펙 변경, 충전소 투영 개선을 할 때 영향 범위가 넓다.
- 외부 자료 기반 근사값과 순수 알고리즘이 섞여 있다.

권장 분리:

```text
backend/app/domain/vehicles.py
  VehicleSpec, DOLPHIN_STANDARD

backend/app/domain/energy.py
  temp_factor, speed_factor, effective_range

backend/app/domain/charging_curve.py
  _vehicle_kw, _charge_minutes

backend/app/domain/geo.py
  haversine, cumulative_distances, sample_path_points

backend/app/services/route_planner.py
  recommend_charging_stops, plan_charging_dp
```

개인 테스트 단계에서는 당장 모두 나눌 필요는 없지만, 테스트가 늘어나기 전에 `vehicles.py`와 `geo.py` 정도는 먼저 분리하는 것이 좋다.

---

## 4.11 `ev_stations.py`가 API 클라이언트, 캐시, 파싱, 접근성 판정, mock을 모두 담당함

위치:

- `backend/app/services/ev_stations.py`

현재 책임:

- 공공 API HTTP 호출
- XML 파싱
- 충전기 상태 매핑
- 충전소 grouping
- 시군구별 카탈로그 캐시
- 실시간 상태 캐시
- 충전소 접근성 판정
- 요금 기본값 관리
- mock 데이터 관리

문제:

- 한 파일이 456라인이다.
- 실 API 장애, XML 구조 변경, 접근 제한 분류, 요금 정책 변경이 모두 같은 파일을 건드린다.
- 공공 API 미제공 값이 실제 데이터처럼 섞인다.

권장 분리:

```text
backend/app/services/ev_api_client.py
  _fetch_rows, _get_with_retry

backend/app/services/ev_station_parser.py
  _group_stations, _build_detail, 코드 매핑

backend/app/services/ev_station_cache.py
  catalog/status cache

backend/app/domain/station_access.py
  _access_class, _availability

backend/app/mocks/ev_stations.py
  _MOCK_STATIONS, _MOCK_DETAIL
```

우선순위:

1. mock 데이터 분리
2. 요금/결제 기본값 분리
3. 접근성 판정 분리
4. API 클라이언트 분리

---

## 4.12 `main/page.tsx`가 화면, 상태, API, 결과 표시를 모두 담당함

위치:

- `frontend/src/app/main/page.tsx`

현재 책임:

- 출발지/도착지 입력 상태
- 자동 기온 조회
- 세션 복원
- 세션 저장
- 경로 계획 API 호출
- 충전소 상태 새로고침
- 결과 카드
- 계산 근거 UI
- 바로가기 버튼
- 여러 SVG/보조 컴포넌트

문제:

- 한 파일이 662라인이다.
- 메인 화면 UI 수정이 경로 계산 상태 관리와 충돌하기 쉽다.
- 개인 바로가기 같은 임시 기능이 핵심 화면에 붙어 있다.

권장 분리:

```text
frontend/src/app/main/page.tsx
  페이지 조립만 담당

frontend/src/features/route-plan/RouteForm.tsx
  출발지/도착지/충전량/기온 입력

frontend/src/features/route-plan/RouteResult.tsx
  결과 카드

frontend/src/features/route-plan/ChargePointList.tsx
  충전소 목록과 대체 충전소 표시

frontend/src/features/route-plan/CalculationBasis.tsx
  계산 근거 표시

frontend/src/features/route-plan/useRoutePlanForm.ts
  상태 복원, 저장, 자동 기온 조회

frontend/src/features/route-plan/useRefreshStationStatus.ts
  상태 새로고침
```

개인 테스트 단계에서는 먼저 `Shortcut`, `Stat`, `AvailBadge`, `CalculationBasis`만 분리해도 가독성이 크게 좋아진다.

---

## 4.13 프론트 `sessionStorage`에 전체 경로 데이터가 저장됨

위치:

- `frontend/src/lib/api.ts`
- `frontend/src/app/map/page.tsx`

문제:

- `haji.routePlan`에 경로 좌표 전체가 저장된다.
- `haji.mainForm`에 출발지/도착지 입력값이 저장된다.
- `haji.selectedCharger`에 선택 충전기 정보가 저장된다.

개인 테스트 앱에서는 허용 가능하지만, 구조적으로는 API 클라이언트 파일이 저장소 책임까지 갖고 있다.

권장:

```text
frontend/src/lib/api.ts
  서버 API 호출만 담당

frontend/src/lib/routePlanStorage.ts
  plan 저장/로드/삭제

frontend/src/lib/formStorage.ts
  form 저장/로드/삭제

frontend/src/lib/chargerStorage.ts
  charger 저장/로드/삭제
```

추가 개선:

- 저장 만료 시각 추가
- `clearCharger()` 추가
- `clearAllLocalRouteData()` 추가

---

## 4.14 실제 로그인 없이 로그인 화면이 존재함

위치:

- `frontend/src/app/login/page.tsx`
- `frontend/src/app/page.tsx`

문제:

- `/`는 `/login`으로 redirect한다.
- `/login`의 버튼은 실제 인증 없이 `/main`으로 이동한다.
- 서비스 구조상 인증 계층이 없는데 로그인 플로우처럼 보인다.

영향:

- 개인 테스트에서는 무해하지만, 앱 구조를 읽는 사람에게 인증이 있는 것으로 오해시킨다.
- 나중에 실제 로그인 도입 시 기존 이름과 역할이 충돌한다.

권장:

- 개인 테스트 앱이면 `/login`을 `/start` 또는 `/intro`로 바꾼다.
- 버튼 문구는 `로그인`이 아니라 `시작하기`로 둔다.
- 실제 로그인 도입 전까지 auth 관련 명칭을 쓰지 않는다.

---

## 4.15 공공 API가 제공하지 않는 정보가 모델에 포함되어 있음

위치:

- `backend/app/models.py`
- `backend/app/services/ev_stations.py`
- `frontend/src/app/stations/[id]/page.tsx`

대상 정보:

- 잔여시간
- 결제 수단
- 회원/비회원 요금
- 주차 무료 여부
- 외부인 이용 가능 여부

문제:

- 일부는 공공 API 미제공 또는 키워드 기반 추정이다.
- 모델과 UI에 들어가 있으므로 실제 데이터처럼 보일 수 있다.
- 테스트 앱으로는 괜찮지만 서비스 기능의 신뢰도를 흐린다.

판정:

- 기능 요구사항에서 나온 항목을 맞추려는 흔적이다.
- 다만 원천 데이터가 없으면 `확정 정보` 모델에 넣는 것보다 `derived/estimated`로 분리하는 것이 맞다.

권장 모델 구조:

```python
class StationDetail(...):
    chargers: list[RealtimeCharger]
    data_source: str
    derived_info: StationDerivedInfo

class StationDerivedInfo(BaseModel):
    payment_methods_estimated: list[str]
    access_class: str
    access_confidence: str
    price_source: str
```

---

## 4.16 문서가 구현과 섞여 있고 일부는 검토 기록 성격임

위치:

- `docs/route-optimization-review.md`
- `README.md`
- `보완사항.md`

문제:

- `route-optimization-review.md`는 구현 배경과 조사 기록이 섞여 있다.
- README는 실행 안내와 정책/용어 검증이 함께 들어 있다.
- 개발자가 필요한 "현재 동작"과 "검토 이력"을 빠르게 구분하기 어렵다.

권장:

```text
README.md
  앱 소개, 실행 방법, 핵심 API만

docs/architecture.md
  전체 구조와 데이터 흐름

docs/route-planning.md
  충전 계산 모델과 한계

docs/data-sources.md
  카카오, 공공데이터, Open-Meteo 데이터 설명

docs/reviews/
  route-optimization-review.md 같은 검토 기록
```

---

## 5. 불필요하거나 정리 대상인 파일/정보

## 5.1 삭제 또는 이동 권장

| 대상 | 판단 | 이유 |
|---|---|---|
| `backend/frontend/` | 삭제 후보 | 실제 프론트 프로젝트는 루트 `frontend/`에 있음 |
| `bydLogo.png` | 이동/삭제 후보 | 동일 로고가 `frontend/public/bydLogo.png`에도 있음 |
| `프레젠테이션1.pptx` | 이동 후보 | 기획 산출물이며 앱 실행에는 불필요 |
| `frontend/tsconfig.tsbuildinfo` | 삭제 후보 | TypeScript 빌드 캐시 |
| `frontend/.next/` | 삭제 가능 | Next 빌드 산출물 |
| `frontend/node_modules/` | 공유 제외 | 의존성 설치 산출물 |
| `backend/.venv/` | 공유 제외 | Python 가상환경 |

## 5.2 코드에서 분리 권장

| 대상 | 위치 | 이유 |
|---|---|---|
| 개인 주소 바로가기 | `main/page.tsx` | 개인 정보성 데이터 |
| mock 장소 목록 | `kakao.py` | 실 API 클라이언트와 혼재 |
| mock 충전소 목록 | `ev_stations.py` | 실 API 클라이언트와 혼재 |
| 요금 기본값 | `ev_stations.py` | 원천 데이터가 아닌 추정값 |
| 충전 커브 상수 | `charging.py` | 차량 데이터 관리 책임 |
| 차량 스펙 | `charging.py`, `charge.ts` | 백엔드/프론트 중복 |

---

## 6. 서비스 기능 기준 권장 구조

현 기능을 기준으로 하면 다음 구조가 더 적당하다.

```text
backend/app/
├─ main.py
├─ config.py
├─ api/
│  ├─ route.py
│  ├─ places.py
│  ├─ stations.py
│  └─ weather.py
├─ schemas/
│  ├─ common.py
│  ├─ route.py
│  ├─ station.py
│  └─ place.py
├─ domain/
│  ├─ vehicles.py
│  ├─ geo.py
│  ├─ energy.py
│  ├─ charging_curve.py
│  └─ station_access.py
├─ services/
│  ├─ route_planner.py
│  ├─ kakao_client.py
│  ├─ ev_api_client.py
│  ├─ ev_station_service.py
│  └─ weather_client.py
└─ mocks/
   ├─ kakao.py
   └─ ev_stations.py
```

프론트는 다음처럼 기능 단위로 나누는 것이 좋다.

```text
frontend/src/
├─ app/
│  ├─ start/
│  ├─ main/
│  ├─ map/
│  ├─ stations/[id]/
│  └─ charge/
├─ features/
│  ├─ route-plan/
│  │  ├─ RouteForm.tsx
│  │  ├─ RouteResult.tsx
│  │  ├─ ChargePointList.tsx
│  │  ├─ CalculationBasis.tsx
│  │  ├─ useRoutePlanForm.ts
│  │  └─ useRefreshStationStatus.ts
│  ├─ station-detail/
│  └─ charge-estimate/
├─ components/
│  ├─ KakaoMap.tsx
│  ├─ LoadingOverlay.tsx
│  └─ CarLoader.tsx
├─ lib/
│  ├─ api.ts
│  ├─ routePlanStorage.ts
│  ├─ formStorage.ts
│  └─ chargerStorage.ts
└─ types/
   └─ api.ts
```

---

## 7. 단계별 정리 순서

## 7.1 바로 정리

1. `backend/frontend/` 삭제 여부 확인 후 제거
2. `main/page.tsx`의 개인 주소 바로가기 제거 또는 개인 설정 파일로 분리
3. README의 340km와 코드의 300km 기준 통일
4. `로그인` 화면/문구를 `시작하기`로 변경
5. `data_source=mock` 표시를 메인/지도/상세 화면에 추가
6. `실시간 충전현황` 문구를 `공공데이터 기준 충전기 상태`로 변경

## 7.2 다음 정리

1. mock 데이터를 `backend/app/mocks`로 이동
2. `frontend/src/lib/api.ts`에서 storage 함수를 분리
3. `main/page.tsx`에서 결과 카드와 계산 근거 컴포넌트 분리
4. `charging.py`에서 차량 스펙과 지리 계산 함수 분리
5. `ev_stations.py`에서 API 클라이언트와 접근성 판정 분리

## 7.3 장기 정리

1. 백엔드에 테스트 추가
2. 충전 계산 API를 단일화해서 프론트 단순 계산과 백엔드 DP 계산 불일치 제거
3. 데이터 출처와 추정값을 모델에서 분리
4. docs 구조를 `architecture`, `route-planning`, `data-sources`, `reviews`로 재정리

---

## 8. 결론

현재 프로젝트는 개인 테스트 앱으로는 충분히 이해 가능한 구조다. 프론트/백엔드 분리와 라우터/서비스 분리는 잘 잡혀 있다.

하지만 서비스 기능 기준으로 보면 다음 네 가지가 가장 큰 구조 결함이다.

1. 실데이터, mock 데이터, 데모 편의 기능이 같은 코드 경로에 섞여 있다.
2. 개인 주소와 브랜드 로고 같은 비기능 정보가 앱 소스에 포함되어 있다.
3. 충전 시간 계산이 백엔드와 프론트에서 서로 다르게 구현되어 있다.
4. 큰 파일이 너무 많은 책임을 갖고 있어 변경 영향 범위가 넓다.

개인 테스트를 계속하더라도 먼저 `개인 정보 제거`, `mock 분리`, `주행거리 기준 통일`, `중복 계산 정리`부터 진행하는 것이 좋다.

