# 웹 취약점 및 접근성 검토

대상 프로젝트: `haji-charge-route`

검토 기준:

- OWASP Top 10:2021
- OWASP API Security Top 10:2023
- WCAG 2.2
- KWCAG 2.2

참고 기준:

- OWASP Top 10은 웹 애플리케이션의 주요 보안 위험을 정리한 표준 인식 문서다.
- OWASP API Security Top 10 2023은 API 보안 위험을 다룬다.
- WCAG 2.2는 장애가 있는 사용자가 웹 콘텐츠에 더 접근 가능하도록 만드는 기준이다.
- KWCAG 2.2는 국내 웹 접근성 지침으로, 4가지 원칙과 세부 검사항목 체계를 가진다.

참고 링크:

- https://owasp.org/Top10/2021/
- https://owasp.org/API-Security/editions/2023/en/0x00-header/
- https://www.w3.org/TR/WCAG22/
- https://a11ykr.github.io/kwcag22/

---

## 1. 요약

현재 앱은 개인 테스트 용도로는 충분히 사용할 수 있지만, 웹 프로그램 관점에서 다음 위험이 있다.

### 보안상 주요 위험

- 백엔드 API에 인증과 요청 제한이 없다.
- 외부 API 호출 실패 시 mock 데이터로 조용히 폴백한다.
- 위치정보와 경로 좌표가 브라우저 `sessionStorage`에 저장된다.
- CORS 설정이 운영 기준으로는 넓다.
- FastAPI 문서가 운영에서 노출될 수 있다.
- 공공 충전소 API가 HTTP로 호출된다.
- 프론트 의존성 감사에서 high 등급 취약점이 발견되었다.
- 개인 주소와 브랜드 로고가 소스와 화면에 포함되어 있다.

### 접근성상 주요 위험

- 모바일 확대가 막혀 있다.
- 장소 검색 모달에 `dialog` 역할, `aria-modal`, 포커스 트랩, ESC 닫기가 없다.
- 클릭 가능한 테이블 행이 키보드로 조작되지 않는다.
- 지도 마커 조작은 키보드/스크린리더 접근성이 약하다.
- 로딩 오버레이가 스크린리더에 상태 변화를 충분히 알리지 않는다.
- 일부 아이콘/이모지 기반 버튼의 의미 전달이 약하다.
- 색상만으로 상태를 구분하는 부분이 있다.

---

## 2. 보안 취약점 검토

## 2.1 인증 없는 API

위치:

- `backend/app/main.py`
- `backend/app/routers/route.py`
- `backend/app/routers/places.py`
- `backend/app/routers/stations.py`
- `backend/app/routers/weather.py`

관련 기준:

- OWASP API2:2023 Broken Authentication
- OWASP API6:2023 Unrestricted Access to Sensitive Business Flows

문제:

- `/api/route/plan`
- `/api/route/status`
- `/api/places/search`
- `/api/stations/{id}`
- `/api/weather/current`

위 API가 모두 인증 없이 호출 가능하다.

개인 로컬 테스트라면 큰 문제는 아니지만, 서버를 외부에 공개하면 누구나 카카오 API, 공공데이터 API, Open-Meteo 호출을 대신 발생시킬 수 있다.

영향:

- API 쿼터 소진
- 비용 발생
- 서비스 장애
- 위치/경로 계산 기능의 남용

권장 보완:

- 개인 테스트: 백엔드를 `127.0.0.1`로만 실행
- 내부 공유: 간단한 API 토큰 헤더 추가
- 외부 공개: 사용자 인증, rate limit, quota, 로그 모니터링 추가

예시 정책:

```text
X-Haji-Test-Key: <개인 테스트 키>
```

---

## 2.2 요청 제한 부재

위치:

- `backend/app/routers/route.py`
- `backend/app/routers/places.py`
- `backend/app/routers/weather.py`
- `backend/app/routers/stations.py`

관련 기준:

- OWASP API4:2023 Unrestricted Resource Consumption
- OWASP API6:2023 Unrestricted Access to Sensitive Business Flows

문제:

- 경로 계산 API는 여러 외부 API 호출과 DP 계산을 수행한다.
- 장소 검색 API는 카카오 검색 API를 호출한다.
- 상세 화면은 10초마다 충전소 상세를 갱신한다.
- 백엔드에 IP별, 엔드포인트별 요청 제한이 없다.

영향:

- 카카오/공공데이터/Open-Meteo 쿼터 소진
- 불필요한 네트워크 비용 증가
- 앱 응답 지연

권장 보완:

- 프론트 버튼 중복 클릭 방지
- `/api/route/plan`: IP별 분당 10회 이하
- `/api/places/search`: IP별 분당 30회 이하
- `/api/route/status`: station id 개수 제한 및 5~10초 쿨다운
- 상세 화면 refetch interval을 사용자가 화면에 머무를 때만 유지

---

## 2.3 CORS 설정 과다 허용

위치:

- `backend/app/main.py`

현재 구조:

```python
allow_origins=[settings.frontend_origin]
allow_methods=["*"]
allow_headers=["*"]
```

관련 기준:

- OWASP A05:2021 Security Misconfiguration
- OWASP API8:2023 Security Misconfiguration

문제:

- origin은 제한되어 있지만 method/header는 전체 허용이다.
- 개인 테스트에서는 괜찮지만 운영 기준으로는 필요한 항목만 허용하는 편이 안전하다.

권장 보완:

```python
allow_methods=["GET", "POST"]
allow_headers=["Content-Type", "Authorization"]
```

추가로 운영에서는 `frontend_origin`을 HTTPS 도메인으로 고정한다.

---

## 2.4 외부 API 실패 시 mock 폴백

위치:

- `backend/app/services/kakao.py`
- `backend/app/services/ev_stations.py`

관련 기준:

- OWASP A04:2021 Insecure Design
- OWASP API10:2023 Unsafe Consumption of APIs

문제:

- 카카오 키가 없거나 검색 실패 시 mock 장소 또는 서울 좌표로 폴백한다.
- 공공 충전소 API 실패 시 mock 충전소로 폴백한다.
- 사용자는 결과가 실제 데이터인지 mock인지 즉시 알기 어렵다.

영향:

- 잘못된 경로 계산
- 실제로 존재하지 않거나 상태가 다른 충전소 추천
- 실주행 테스트 시 위험한 판단 유도

권장 보완:

- `MOCK_ENABLED` 설정을 명시적으로 둔다.
- API 키가 있는데 실패한 경우에는 mock으로 폴백하지 않는다.
- 응답에 `data_source`뿐 아니라 `data_quality` 또는 `is_mock` 필드를 둔다.
- 프론트에서 mock 데이터일 때 경고 배너를 표시한다.

---

## 2.5 지오코딩 실패 시 서울 기본값

위치:

- `backend/app/services/kakao.py`

관련 기준:

- OWASP A04:2021 Insecure Design

문제:

- 주소를 찾지 못하면 최종적으로 서울 좌표를 반환할 수 있다.
- 오류를 숨기고 정상 계산처럼 이어지는 구조다.

영향:

- 출발지/도착지가 잘못 계산된다.
- 충전소 추천 결과가 무의미해진다.

권장 보완:

- 실데이터 모드에서는 지오코딩 실패 시 400 오류를 반환한다.
- mock 모드에서만 대표 위치 폴백을 허용한다.

권장 사용자 문구:

```text
주소를 찾을 수 없습니다. 출발지 또는 도착지를 다시 선택해 주세요.
```

---

## 2.6 위치정보와 경로 좌표의 브라우저 저장

위치:

- `frontend/src/lib/api.ts`

저장 대상:

- `haji.routePlan`
- `haji.mainForm`
- `haji.selectedCharger`

관련 기준:

- OWASP A02:2021 Cryptographic Failures
- OWASP A01:2021 Broken Access Control

문제:

- 출발지, 도착지, 경로 좌표, 충전소 선택 정보가 `sessionStorage`에 저장된다.
- XSS가 발생하면 저장된 위치정보를 읽을 수 있다.
- 공용 PC나 화면 공유 환경에서 민감한 이동 정보가 남을 수 있다.

개인 테스트 기준:

- `sessionStorage`는 브라우저 탭 단위라 `localStorage`보다 낫다.
- 그래도 경로 좌표 전체 저장은 민감할 수 있다.

권장 보완:

- 저장 만료 시각 추가
- 앱 종료/초기화 시 전체 삭제
- `clearCharger()`와 `clearAllRouteData()` 추가
- 전체 경로 좌표 대신 필요한 최소 정보만 저장

---

## 2.7 입력 길이 제한 부족

위치:

- `backend/app/models.py`
- `backend/app/routers/places.py`
- `backend/app/routers/weather.py`

관련 기준:

- OWASP A03:2021 Injection
- OWASP API4:2023 Unrestricted Resource Consumption

문제:

- `current_charge_pct`, `temperature_c`는 범위 제한이 있다.
- 그러나 `origin`, `destination`, `query`는 최소 길이 외에 최대 길이가 없다.

영향:

- 매우 긴 검색어로 외부 API 요청이 발생할 수 있다.
- 로그가 비대해질 수 있다.
- 예외 처리 비용이 늘 수 있다.

권장 보완:

```python
origin: str = Field(..., min_length=1, max_length=200)
destination: str = Field(..., min_length=1, max_length=200)
```

`places.search`와 `weather.current`의 `Query`에도 `max_length=200`을 둔다.

---

## 2.8 공공 API HTTP 호출

위치:

- `backend/app/services/ev_stations.py`

현재:

```python
EV_API_BASE = "http://apis.data.go.kr/B552584/EvCharger"
```

관련 기준:

- OWASP A02:2021 Cryptographic Failures
- OWASP API10:2023 Unsafe Consumption of APIs

문제:

- HTTPS가 아닌 HTTP 엔드포인트를 사용한다.
- 서버 간 요청이지만 네트워크 구간에서 변조나 감청 가능성을 완전히 배제할 수 없다.

권장 보완:

- 공공데이터포털에서 HTTPS 지원 URL을 확인한다.
- HTTPS가 가능하면 교체한다.
- 불가능하면 응답 데이터 검증과 장애 시 오류 표시를 강화한다.

---

## 2.9 운영 문서 노출 가능성

위치:

- `backend/app/main.py`

관련 기준:

- OWASP A05:2021 Security Misconfiguration
- OWASP API9:2023 Improper Inventory Management

문제:

- FastAPI 기본 설정은 `/docs`, `/redoc`, `/openapi.json` 문서를 노출한다.
- 개인 테스트에서는 유용하지만, 외부 공개 시 API 구조가 그대로 드러난다.

권장 보완:

- 개인 테스트: 유지 가능
- 외부 공개: `APP_ENV=production`일 때 문서 비활성화

예:

```python
app = FastAPI(
    title="Haji Charge Route API",
    version="0.1.0",
    docs_url=None if settings.is_prod else "/docs",
    redoc_url=None if settings.is_prod else "/redoc",
    openapi_url=None if settings.is_prod else "/openapi.json",
)
```

---

## 2.10 취약한 의존성

위치:

- `frontend/package.json`
- `frontend/package-lock.json`

점검 결과:

- `npm audit --omit=dev` 기준 high 등급 취약점 3건이 확인되었다.
- Next 하위 의존성의 PostCSS, sharp 관련 취약점이다.

관련 기준:

- OWASP A06:2021 Vulnerable and Outdated Components

권장 보완:

- `npm audit` 재실행
- Next, sharp, PostCSS 관련 업데이트
- 업데이트 후 `npm run build`와 기능 테스트 수행
- 외부 공개 전에는 취약점 0 또는 수용 가능한 수준까지 낮춘다.

---

## 2.11 보안 헤더 부재

위치:

- `frontend/next.config.ts`
- `backend/app/main.py`

관련 기준:

- OWASP A05:2021 Security Misconfiguration

문제:

- CSP, HSTS, Referrer-Policy, X-Content-Type-Options 등 명시적 보안 헤더 설정이 없다.

개인 로컬 테스트에서는 영향이 작지만, 외부 공개 시 필요하다.

권장 보완:

- Next.js `headers()` 설정 또는 배포 프록시에서 보안 헤더 추가
- 카카오 지도 스크립트 도메인을 고려한 CSP 설계

예상 헤더:

```text
X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin
Content-Security-Policy: default-src 'self'; ...
Strict-Transport-Security: max-age=31536000; includeSubDomains
```

---

## 2.12 개인 주소와 브랜드 자산 노출

위치:

- `frontend/src/app/main/page.tsx`
- `frontend/src/app/login/page.tsx`
- `bydLogo.png`
- `frontend/public/bydLogo.png`

문제:

- 개인 주소성 바로가기와 BYD 로고가 앱 코드에 포함되어 있다.
- 보안 취약점이라기보다 정보 노출과 오인 위험이다.

권장 보완:

- 개인 주소는 소스에서 제거하고 개인 설정 파일로 분리
- 공개 가능성이 있으면 BYD 로고를 자체 아이콘으로 교체
- 실제 인증이 없으면 `로그인`을 `시작하기`로 변경

---

## 3. 접근성 검토

## 3.1 모바일 확대 금지

위치:

- `frontend/src/app/layout.tsx`

현재:

```typescript
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
};
```

관련 기준:

- WCAG 1.4.4 Resize Text
- KWCAG 확대 기능 관련 접근성 원칙

문제:

- `maximumScale: 1`은 사용자가 모바일 브라우저에서 확대하는 것을 제한할 수 있다.
- 저시력 사용자가 글자를 키워 보기 어렵다.

권장 보완:

- `maximumScale`을 제거한다.
- 최소한 200% 확대 시 주요 기능이 깨지지 않는지 확인한다.

권장:

```typescript
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};
```

---

## 3.2 장소 검색 모달의 dialog 접근성 부족

위치:

- `frontend/src/components/PlaceSearchModal.tsx`

관련 기준:

- WCAG 2.1.1 Keyboard
- WCAG 2.4.3 Focus Order
- WCAG 4.1.2 Name, Role, Value
- KWCAG 키보드 사용 보장, 초점 이동

문제:

- 모달 컨테이너에 `role="dialog"`가 없다.
- `aria-modal="true"`가 없다.
- 제목과 모달을 연결하는 `aria-labelledby`가 없다.
- ESC 닫기 처리가 없다.
- 포커스 트랩이 없다.
- 닫을 때 이전 포커스로 되돌리는 로직이 없다.

영향:

- 스크린리더가 모달을 일반 영역처럼 읽을 수 있다.
- 키보드 사용자가 모달 밖으로 포커스가 빠져 혼란을 겪을 수 있다.

권장 보완:

```tsx
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby="place-search-title"
>
  <h2 id="place-search-title">{title} 검색</h2>
</div>
```

추가:

- ESC 키 닫기
- Tab/Shift+Tab 포커스 트랩
- 닫은 후 열기 버튼으로 포커스 복귀

---

## 3.3 모달 배경 클릭 닫기만으로는 부족

위치:

- `frontend/src/components/PlaceSearchModal.tsx`

문제:

- 배경 클릭으로 닫히지만 키보드 사용자는 ESC 닫기가 없다.
- 닫기 버튼은 있으나 모달 전체 상태가 스크린리더에 충분히 전달되지 않는다.

권장 보완:

- ESC 키 닫기
- 닫기 버튼 텍스트를 시각적으로 숨긴 텍스트와 함께 제공
- 모달 열림 시 첫 입력 필드에 초점 이동은 유지하되, 닫힘 시 이전 버튼에 복귀

---

## 3.4 클릭 가능한 테이블 행이 키보드로 조작되지 않음

위치:

- `frontend/src/app/stations/[id]/page.tsx`

현재:

- 충전기 행 `<tr>`에 `onClick`이 있다.
- 하지만 `<tr>`은 기본적으로 키보드 포커스를 받지 않는다.
- Enter/Space 키 조작도 없다.

관련 기준:

- WCAG 2.1.1 Keyboard
- WCAG 4.1.2 Name, Role, Value
- KWCAG 키보드 사용 보장

영향:

- 키보드 사용자는 충전기를 선택하기 어렵다.
- 스크린리더 사용자는 행이 선택 가능한 요소인지 알기 어렵다.

권장 보완:

가장 좋은 방식은 행 내부에 버튼을 넣는 것이다.

```tsx
<td>
  <button onClick={...}>
    1번 충전기 선택
  </button>
</td>
```

또는 행을 유지하려면:

```tsx
<tr
  tabIndex={0}
  role="button"
  onKeyDown={(e) => {
    if (e.key === "Enter" || e.key === " ") selectCharger();
  }}
>
```

단, 표 접근성은 행 안 버튼 방식이 더 명확하다.

---

## 3.5 지도 마커의 키보드/스크린리더 접근성 부족

위치:

- `frontend/src/components/KakaoMap.tsx`

관련 기준:

- WCAG 2.1.1 Keyboard
- WCAG 1.1.1 Non-text Content
- WCAG 4.1.2 Name, Role, Value

문제:

- 카카오 지도 마커는 마우스 클릭 중심이다.
- 마커 `title`은 있지만 스크린리더/키보드 접근이 충분한지 보장하기 어렵다.
- 지도만으로 충전소 선택을 제공하면 일부 사용자가 기능을 사용할 수 없다.

현재 긍정적 요소:

- 카카오 키가 없을 때 목록 폴백이 있다.

권장 보완:

- 지도 화면에서도 항상 하단 또는 별도 패널에 충전소 목록을 제공한다.
- 목록의 각 충전소는 `<button>` 또는 `<a>`로 제공한다.
- 마커 클릭과 목록 선택이 같은 동작을 하도록 맞춘다.

---

## 3.6 로딩 오버레이 상태 알림 부족

위치:

- `frontend/src/components/LoadingOverlay.tsx`
- `frontend/src/components/CarLoader.tsx`

관련 기준:

- WCAG 4.1.3 Status Messages
- WCAG 2.2.2 Pause, Stop, Hide

문제:

- 로딩 오버레이가 시각적으로만 나타난다.
- 스크린리더에 로딩 상태가 명확히 알려지지 않는다.
- 애니메이션은 `prefers-reduced-motion`을 고려하고 있어 그 점은 좋다.

권장 보완:

```tsx
<div
  role="status"
  aria-live="polite"
  aria-busy="true"
>
  <CarLoader size={44} label="불러오는 중..." />
</div>
```

또는 오버레이 컨테이너에 `aria-modal` 성격이 필요한지 검토한다.

---

## 3.7 색상만으로 상태 구분 가능성

위치:

- `frontend/src/app/main/page.tsx`
- `frontend/src/app/stations/[id]/page.tsx`

관련 기준:

- WCAG 1.4.1 Use of Color
- KWCAG 색에 무관한 콘텐츠 인식

현재:

- 충전 가능/불가 상태는 색상과 텍스트를 함께 보여준다.
- 이 점은 긍정적이다.

남은 문제:

- 점 아이콘 색상과 상태 배지 색상 의존도가 높다.
- 지도 마커는 충전소 상태를 색상으로 구분할 경우 텍스트 대안이 필요하다.

권장 보완:

- 모든 상태에 텍스트를 함께 유지한다.
- 아이콘에는 `aria-label` 또는 주변 텍스트를 둔다.
- 지도 목록에서는 `사용가능`, `점검중`, `상태미확인` 텍스트를 반드시 표시한다.

---

## 3.8 입력 필드와 버튼의 접근성

위치:

- `frontend/src/app/main/page.tsx`
- `frontend/src/app/charge/page.tsx`
- `frontend/src/components/PlaceSearchModal.tsx`

관련 기준:

- WCAG 3.3.2 Labels or Instructions
- WCAG 2.5.3 Label in Name

긍정적 요소:

- `Field` 컴포넌트가 `<label>`로 감싸는 구조라 기본 입력 필드 접근성은 괜찮다.
- 뒤로/닫기/새로고침 같은 일부 아이콘 버튼에는 `aria-label`이 있다.

문제:

- 장소 선택 버튼은 시각적으로 입력처럼 보이지만 실제는 버튼이다. 이 자체는 괜찮지만 `aria-haspopup="dialog"`를 추가하면 더 명확하다.
- 충전량 입력은 숫자 범위가 화면에 충분히 설명되지 않는다.
- 충전 계산 화면의 단위 토글 버튼은 현재 선택 상태를 `aria-pressed`로 표현하지 않는다.

권장 보완:

```tsx
<button aria-haspopup="dialog">
  출발지 선택
</button>
```

단위 토글:

```tsx
<button aria-pressed={unit === u}>
  %
</button>
```

---

## 3.9 접힘 영역의 접근성 정보 부족

위치:

- `frontend/src/app/main/page.tsx`

관련 기준:

- WCAG 4.1.2 Name, Role, Value

현재:

- 결과 영역과 계산근거 영역에 `aria-expanded`가 있다.

보완점:

- `aria-controls`로 실제 접힘 영역 id를 연결하면 더 좋다.
- 접힌 상태에서 중요한 결과가 숨겨질 경우 요약 정보는 버튼 텍스트 안에 유지한다. 현재 요약 정보가 있어 이 부분은 괜찮다.

권장:

```tsx
<button
  aria-expanded={resultOpen}
  aria-controls="route-result-panel"
>
```

---

## 3.10 에러 메시지와 상태 메시지의 알림 부족

위치:

- `frontend/src/app/main/page.tsx`
- `frontend/src/components/PlaceSearchModal.tsx`
- `frontend/src/app/stations/[id]/page.tsx`

관련 기준:

- WCAG 3.3.1 Error Identification
- WCAG 4.1.3 Status Messages

문제:

- 에러 메시지는 화면에 표시되지만 `role="alert"` 또는 `aria-live`가 없다.
- 검색 결과 없음, 검색 중, 계산 중 상태도 스크린리더에 충분히 전달되지 않을 수 있다.

권장 보완:

```tsx
{error && <p role="alert">...</p>}
```

검색 결과 영역:

```tsx
<div aria-live="polite">
  검색 중...
</div>
```

---

## 3.11 애니메이션 접근성

위치:

- `frontend/src/components/CarLoader.tsx`

관련 기준:

- WCAG 2.2.2 Pause, Stop, Hide
- WCAG 2.3.3 Animation from Interactions

긍정적 요소:

- `prefers-reduced-motion: reduce`를 반영해 애니메이션을 끄는 CSS가 있다.

보완점:

- SVG가 장식용이면 `aria-hidden="true"`를 적용하는 것이 좋다.
- 로딩 텍스트는 `role="status"` 영역 안에 두는 것이 좋다.

---

## 3.12 테이블 캡션 부재

위치:

- `frontend/src/app/stations/[id]/page.tsx`

관련 기준:

- WCAG 1.3.1 Info and Relationships

문제:

- 충전기 상태 표에 `<caption>`이 없다.
- 표 위에 제목 텍스트가 있지만, 스크린리더에서는 표와 제목 관계가 명시적이지 않을 수 있다.

권장 보완:

```tsx
<table>
  <caption className="sr-only">
    충전기별 커넥터, 충전방식, 충전 상태, 잔여시간
  </caption>
</table>
```

---

## 3.13 작은 모바일 컨테이너 고정

위치:

- `frontend/src/app/globals.css`

현재:

```css
.phone-shell {
  max-width: 430px;
  margin: 0 auto;
  min-height: 100dvh;
}
```

문제:

- 모바일 앱처럼 보이게 하는 구조는 의도에 맞다.
- 그러나 데스크톱 확대, 큰 글자 설정, 긴 텍스트에서 공간이 부족할 수 있다.

권장 보완:

- 200% 확대와 긴 주소/충전소명 테스트
- 버튼/배지 내부 텍스트 줄바꿈 허용
- 지도와 표는 가로 스크롤 또는 목록 대체를 명확히 제공

---

## 4. 우선순위별 조치 목록

## 4.1 P0 - 개인 테스트라도 먼저 처리

1. `frontend/src/app/layout.tsx`에서 `maximumScale: 1` 제거
2. `frontend/src/app/main/page.tsx`의 개인 주소 바로가기 제거
3. `backend/app/services/kakao.py`의 서울 기본 폴백 제거 또는 mock 모드 한정
4. mock 데이터 사용 시 화면에 명확한 경고 표시
5. `실시간 충전현황` 문구를 `공공데이터 기준 충전기 상태`로 변경
6. 충전기 상세 표의 클릭 행을 버튼 또는 키보드 조작 가능 요소로 변경
7. 장소 검색 모달에 `role="dialog"`, `aria-modal`, ESC 닫기, 포커스 복귀 추가

## 4.2 P1 - 외부 공유 전 처리

1. API 요청 제한 추가
2. CORS method/header 제한
3. 입력 문자열 `max_length` 추가
4. FastAPI `/docs` 운영 비활성화 옵션 추가
5. `sessionStorage` 만료와 전체 초기화 추가
6. 보안 헤더 추가
7. `npm audit` high 취약점 업데이트
8. 지도 화면에 항상 접근 가능한 충전소 목록 제공

## 4.3 P2 - 품질 개선

1. 충전 시간 계산을 백엔드 API로 단일화
2. 데이터 출처와 mock 여부를 모든 화면에 표시
3. 표에 caption 추가
4. 로딩/에러 상태에 `aria-live`, `role="alert"` 적용
5. 단위 토글에 `aria-pressed` 적용
6. 접힘 영역에 `aria-controls` 연결

---

## 5. 권장 코드 변경 예시

## 5.1 viewport 수정

파일:

- `frontend/src/app/layout.tsx`

변경 전:

```typescript
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
};
```

변경 후:

```typescript
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};
```

## 5.2 모달 접근성 보완

파일:

- `frontend/src/components/PlaceSearchModal.tsx`

핵심 변경:

```tsx
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby="place-search-title"
>
  <h2 id="place-search-title">{title} 검색</h2>
</div>
```

추가 필요:

- ESC 닫기
- 포커스 트랩
- 닫은 뒤 이전 포커스로 복귀

## 5.3 충전기 행을 버튼으로 변경

파일:

- `frontend/src/app/stations/[id]/page.tsx`

권장:

```tsx
<button
  type="button"
  onClick={() => selectCharger(c)}
  className="..."
>
  {c.charger_no}번 충전기 선택
</button>
```

행 전체 클릭 UX를 유지하려면 최소한 `tabIndex`, `role`, `onKeyDown`을 추가한다.

## 5.4 에러 메시지 접근성

```tsx
{mutation.isError && (
  <p role="alert" className="text-sm text-red-500">
    {(mutation.error as Error).message}
  </p>
)}
```

## 5.5 백엔드 입력 길이 제한

파일:

- `backend/app/models.py`

```python
origin: str = Field(..., min_length=1, max_length=200)
destination: str = Field(..., min_length=1, max_length=200)
```

파일:

- `backend/app/routers/places.py`

```python
query: str = Query(..., min_length=1, max_length=200)
```

---

## 6. 결론

현재 앱은 개인 테스트 앱으로는 큰 틀에서 사용할 수 있지만, 웹 프로그램으로 보면 보안과 접근성의 기본 장치가 일부 빠져 있다.

가장 먼저 고칠 항목은 다음이다.

1. 모바일 확대 금지 제거
2. 개인 주소 제거
3. mock 데이터와 실데이터 구분 표시
4. 장소 검색 모달 접근성 보완
5. 충전기 선택 테이블의 키보드 접근성 보완
6. API 요청 제한과 입력 길이 제한 추가

이 여섯 가지를 먼저 처리하면 개인 테스트 단계에서도 안전성과 사용성이 크게 좋아진다.

