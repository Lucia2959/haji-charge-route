fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0
# 혼잡 예측용 Postgres 드라이버. DATABASE_URL을 안 쓰면 import되지 않으므로
# 설치가 실패해도(휠 미제공 등) 앱은 그대로 뜬다 — app/db.py 참고.
asyncpg>=0.30
