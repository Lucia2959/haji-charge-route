# 개발 시 보완사항

대상 앱: BYD 돌핀 스탠다드 기준 충전 경로 안내 테스트 앱

이 문서는 상용 배포 전 단계가 아니라, 개인이 개발·테스트하면서 안전하게 사용하기 위한 보완사항을 정리한다. 핵심 목표는 다음 네 가지다.

- 외부 공개 차단
- API 키와 개인 주소 보호
- mock/실데이터 혼동 방지
- 충전 계획 결과를 참고값으로 명확히 표시

---

## 1. 우선순위 요약

### P0 - 바로 보완

- 실제 개인 주소를 소스에서 제거한다.
- `.env`, `.env.local`이 Git에 올라가지 않도록 유지한다.
- 백엔드는 `127.0.0.1` 또는 신뢰 가능한 내부망에서만 실행한다.
- README와 코드의 BYD 돌핀 주행거리 기준을 통일한다.
- 실데이터 실패 시 mock으로 조용히 바뀌는 동작을 화면에 명확히 표시한다.
- "실시간" 문구를 "공공데이터 기준 상태" 또는 "최근 조회 상태"로 완화한다.

### P1 - 개발 중 적용 권장

- 카카오 지오코딩 실패 시 서울로 폴백하지 않고 오류를 보여준다.
- `/api/route/plan`, `/api/places/search`, `/api/route/status`에 간단한 rate limit을 둔다.
- 프론트 `sessionStorage`에 저장된 경로·주소 데이터를 초기화/만료시킨다.
- 충전 계산 화면의 단순 계산과 백엔드 DP 계산 방식 차이를 표시한다.
- API 키가 없거나 실패한 경우의 데이터 출처를 UI에 노출한다.

### P2 - 품질 개선

- 주요 경로 테스트 케이스를 단위 테스트로 만든다.
- 차량 스펙과 충전 커브 값을 별도 설정 파일로 분리한다.
- 충전 요금/결제 수단은 실제 출처가 없으면 숨기거나 "참고값"으로 표시한다.
- 오픈소스 라이선스 고지 파일을 준비한다.

---

## 2. 파일별 보완사항

## 2.1 백엔드 공통

파일:

- `backend/app/main.py`
- `backend/app/config.py`

### 보완사항

1. CORS 설정을 개발 도메인으로만 제한한다.

현재 `allow_origins`는 `settings.frontend_origin`만 허용하지만, `allow_methods`와 `allow_headers`가 전체 허용이다.

개발 단계에서는 큰 문제는 아니지만, 외부 공개 가능성이 생기면 다음처럼 제한한다.

```python
allow_methods=["GET", "POST"]
allow_headers=["Content-Type", "Authorization"]
```

2. 운영/개발 모드를 분리한다.

개인 테스트 앱이라도 다음 설정값을 추가하면 mock과 실데이터 혼동을 줄일 수 있다.

```env
APP_ENV=development
MOCK_ENABLED=true
```

상용 또는 실주행 테스트에 가까운 모드에서는 `MOCK_ENABLED=false`로 두고, 외부 API 실패 시 명확한 오류를 반환하도록 한다.

3. 백엔드 실행 호스트를 로컬로 제한한다.

개발 실행은 다음처럼 한다.

```bash
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

`0.0.0.0`로 실행하면 같은 네트워크의 다른 기기에서 접근할 수 있다. 개인 테스트 목적이면 피하는 것이 좋다.

---

## 2.2 환경 변수와 API 키

파일:

- `backend/.env`
- `backend/.env.example`
- `frontend/.env.local`
- `frontend/.env.local.example`
- `.gitignore`

### 보완사항

1. 실제 키 파일은 계속 Git 제외한다.

현재 `.gitignore`에는 다음 항목이 있어 방향은 좋다.

```gitignore
.env
.env.local
.env.*.local
```

추가로 커밋 전 다음 명령으로 키 노출 여부를 확인한다.

```bash
rg -n "KAKAO|API_KEY|SERVICE_KEY|SECRET|TOKEN|Authorization" -g "!frontend/node_modules/**"
```

2. 카카오 Developers 허용 도메인을 제한한다.

개인 테스트에서는 JavaScript 키 허용 도메인을 다음 정도로 제한한다.

- `http://localhost:3000`
- 필요 시 개발 PC의 내부망 주소 1개

사용하지 않는 도메인, 와일드카드, 오래된 테스트 도메인은 제거한다.

3. 키가 유출되었으면 재발급한다.

GitHub, 블로그, 캡처, 영상, 로그에 키가 노출된 적이 있으면 해당 키는 폐기하고 재발급한다.

---

## 2.3 경로 계획 API

파일:

- `backend/app/routers/route.py`

### 보완사항

1. 입력값 길이와 형식을 제한한다.

`origin`, `destination`은 주소·지명 문자열이므로 너무 긴 입력을 막는다. 모델에서 `max_length`를 주는 방식이 좋다.

예:

```python
origin: str = Field(..., min_length=1, max_length=200)
destination: str = Field(..., min_length=1, max_length=200)
```

2. 외부 API 호출 실패와 mock 폴백을 구분한다.

현재 앱은 키가 없거나 실패하면 mock 데이터로 동작한다. 개발 데모에는 편하지만, 실제 경로 테스트에서는 위험하다.

권장 동작:

- 키가 아예 없을 때: mock 사용 가능, 화면에 "mock 데이터" 표시
- 키가 있는데 외부 API 실패: 오류 표시
- 실주행 테스트 모드: mock 폴백 금지

3. 충전 가능 여부 표현을 완화한다.

`feasible=true`는 "계산상 완주 가능"이라는 뜻이지, 실제 완주 보장이 아니다.

화면 문구 예:

```text
계산상 완주 가능
공공데이터와 근사 주행거리 기준이며, 실제 충전 가능 여부는 현장 상태를 확인하세요.
```

4. 상태 새로고침 요청에 제한을 둔다.

`/api/route/status`는 충전소 ID 목록을 받아 외부 API 상태를 갱신한다. 버튼 연타나 자동 호출이 많아지면 쿼터를 소모할 수 있다.

개발 단계에서는 다음 정도로 충분하다.

- 프론트 버튼에 5~10초 쿨다운
- 백엔드에서 station id 개수 제한
- 동일 IP 기준 간단한 초당 요청 제한

---

## 2.4 충전 계산 로직

파일:

- `backend/app/services/charging.py`
- `frontend/src/lib/charge.ts`

### 보완사항

1. BYD 돌핀 주행거리 기준을 통일한다.

현재 코드 기준:

```python
range_km=300.0
capacity_kwh=44.9
```

README에는 340km로 적힌 부분이 있다. 개발 테스트에서는 다음 중 하나로 정한다.

- 인증 기준 주행거리
- 실주행 보수 기준
- 사용자가 직접 조정하는 개인 기준

추천은 개인 테스트 목적상 보수적인 `300km`를 유지하되, README와 화면에 동일하게 표시하는 것이다.

2. 충전 커브는 "근사값"으로 표시한다.

`_CHARGE_CURVE`는 실제 차량·배터리 온도·충전기 출력·SoC에 따라 달라진다.

개발 단계 문구:

```text
충전 시간은 BYD 돌핀 스탠다드 추정 충전 커브 기반 참고값입니다.
```

3. 프론트와 백엔드 충전 시간 계산 방식을 맞춘다.

백엔드 `plan_charging_dp()`는 충전 커브를 반영한다.

프론트 `estimateCharge()`는 단순 계산이다.

```typescript
minutes = chargeKwh / charger.power_kw * 60
```

같은 앱 안에서 예상 시간이 다르게 보일 수 있으므로 다음 중 하나로 정리한다.

- 프론트에도 충전 커브 계산을 적용한다.
- 충전 계산 화면에 "단순 정출력 계산"이라고 표시한다.
- 충전 시간 계산을 백엔드 API로 통합한다.

4. 테스트 케이스를 만든다.

우선 다음 케이스를 고정한다.

- 서울 -> 부산, 50%, 20도
- 서울 -> 부산, 80%, 20도
- 서울 -> 부산, 50%, 영하 10도
- 짧은 거리, 충전 불필요
- 현충전량 0%, 10%, 100%
- 충전소 후보가 없는 경로
- 목적지 인근 충전소가 있는 경우

---

## 2.5 카카오 API 연동

파일:

- `backend/app/services/kakao.py`
- `frontend/src/components/KakaoMap.tsx`

### 보완사항

1. 지오코딩 실패 시 서울 폴백을 제거한다.

현재 키가 없거나 검색 결과가 없으면 mock 장소 또는 서울로 떨어질 수 있다. 개인 테스트에서도 잘못된 경로를 계산할 수 있으므로, 실데이터 모드에서는 명확한 오류를 반환한다.

권장 문구:

```text
출발지 또는 도착지를 찾을 수 없습니다. 주소를 다시 입력해 주세요.
```

2. 직선거리 폴백을 명확히 표시한다.

카카오 길찾기 실패 시 직선거리 x 1.3으로 경로를 만든다. 이 경우 실제 도로 경로가 아니므로 지도와 충전 추천이 크게 어긋날 수 있다.

응답의 `data_source`가 `mock`이면 화면에 다음을 표시한다.

```text
실제 도로 경로를 찾지 못해 추정 경로로 계산했습니다.
```

3. 지도 키 미설정 상태를 크게 표시한다.

현재 지도 키가 없으면 목록 폴백이 표시된다. 개인 테스트에서는 좋지만, 지도 없는 상태의 결과를 실제 경로처럼 오해하지 않게 표시한다.

---

## 2.6 공공 충전소 API

파일:

- `backend/app/services/ev_stations.py`
- `frontend/src/app/stations/[id]/page.tsx`

### 보완사항

1. "실시간" 표현을 완화한다.

공공 API 응답은 원천 데이터의 지연이 있을 수 있다. 앱의 10초 refetch는 서버를 다시 부르는 것일 뿐, 실제 충전기 상태가 즉시 갱신된다는 보장은 아니다.

권장 문구:

```text
공공데이터 기준 최근 충전기 상태
```

2. mock 상태를 화면에 표시한다.

`data_source="mock"`이면 충전소 상세 화면 상단에 다음을 표시한다.

```text
테스트용 mock 데이터입니다. 실제 충전소 상태가 아닙니다.
```

3. 결제 수단과 요금은 참고값으로 표시한다.

공공 API가 제공하지 않는 값은 기본값으로 채워져 있다.

대상 값:

- `_DEFAULT_PAYMENT_METHODS`
- `_UNIT_PRICE`
- mock의 회원/비회원 요금

개발 단계에서는 다음 중 하나를 선택한다.

- 값 숨김
- "참고 단가" 표시
- 직접 확인한 데이터 소스가 있을 때만 표시

4. 외부인 이용 가능 판정은 보수적으로 표시한다.

`limitDetail` 키워드 기반 분류는 실제 현장 정책과 다를 수 있다.

권장 문구:

```text
외부인 이용 가능 추정 - 방문 전 현장 안내를 확인하세요.
```

5. HTTP API 위험을 인지한다.

현재 공공 API 주소는 `http://`이다. 개인 테스트에서는 큰 문제 없이 쓸 수 있지만, 외부 공개 또는 상용 운영 전에는 HTTPS 지원 여부를 확인한다.

---

## 2.7 날씨 API

파일:

- `backend/app/services/weather.py`
- `backend/app/routers/weather.py`
- `frontend/src/app/main/page.tsx`

### 보완사항

1. Open-Meteo는 개발 테스트용으로 사용한다.

코드 주석에도 적혀 있듯 상업 배포 시 상업 플랜 또는 다른 기상 API 검토가 필요하다.

개인 테스트에서는 현재 구조를 유지해도 된다.

2. 자동 기온 입력 출처를 표시한다.

현재 화면에는 "출발지 현재 기온 자동입력" 문구가 있다. 여기에 출처를 추가하면 좋다.

```text
Open-Meteo 기준 출발지 현재 기온 자동입력
```

3. 날씨 실패 시 기존값 유지 동작을 표시한다.

날씨 API 실패 시 `None`을 반환하고 프론트는 기존 값을 유지한다. 개발 중 혼동을 줄이려면 실패 시 작은 안내를 표시한다.

---

## 2.8 프론트 저장소

파일:

- `frontend/src/lib/api.ts`

### 보완사항

1. `sessionStorage` 저장 내용을 최소화한다.

현재 저장 대상:

- `haji.routePlan`: 경로 계획 전체, 경로 좌표 포함
- `haji.mainForm`: 출발지/도착지/충전량/기온
- `haji.selectedCharger`: 충전기 정보

개인 PC에서는 허용 가능하지만, 화면 공유·공용 PC·저장소 업로드에는 주의한다.

2. 앱 초기화 시 모든 저장값을 삭제한다.

현재 `clearPlan`, `clearForm`은 있다. 추가로 충전기 선택값도 지우는 함수를 만든다.

```typescript
export function clearCharger() {
  sessionStorage.removeItem(CHARGER_KEY);
}
```

3. 저장 만료를 둔다.

개인 테스트용으로는 1~3시간 만료면 충분하다. 저장 시각을 함께 넣고 오래된 계획은 자동 삭제한다.

---

## 2.9 메인 화면

파일:

- `frontend/src/app/main/page.tsx`

### 보완사항

1. 개인 주소 바로가기를 제거하거나 환경변수화한다.

현재 바로가기 버튼에 실제 주소처럼 보이는 값이 들어 있다.

보완 방향:

- 소스에서는 제거
- `.env.local`에 개인용 주소 저장
- 화면 라벨은 "집", "회사"처럼 일반화
- GitHub 공유 전 반드시 삭제

2. "로그인" 없는 앱 흐름에 맞게 문구를 정리한다.

개인 테스트 앱이면 인증이 없으므로 "로그인"보다 "시작하기"가 자연스럽다.

3. 계산 결과 하단에 참고 고지를 추가한다.

문구 예:

```text
이 결과는 차량 스펙, 날씨, 공공 충전소 데이터, 근사 충전 커브를 바탕으로 한 참고 계산입니다. 실제 주행 가능 거리와 충전 가능 여부는 도로·날씨·배터리 상태·현장 상황에 따라 달라질 수 있습니다.
```

4. 충전소 상태 새로고침 버튼에 쿨다운을 둔다.

버튼 연타로 외부 API 호출이 늘어나는 것을 막는다.

---

## 2.10 지도 화면

파일:

- `frontend/src/app/map/page.tsx`
- `frontend/src/components/KakaoMap.tsx`

### 보완사항

1. mock 경로 표시를 명확히 한다.

`plan.data_source === "mock"`이면 지도 또는 목록 상단에 경고를 표시한다.

```text
추정 경로입니다. 실제 도로 경로와 다를 수 있습니다.
```

2. 충전소 마커의 상태를 지도에 반영한다.

현재 충전소 마커는 동일한 아이콘이다. 개발 중 확인성을 높이려면 다음처럼 구분한다.

- 사용 가능: 초록
- 사용 불가: 빨강
- 상태 미확인: 회색
- 대체 충전소: 파랑 또는 보조 마커

---

## 2.11 충전소 상세 화면

파일:

- `frontend/src/app/stations/[id]/page.tsx`

### 보완사항

1. 데이터 출처를 표시한다.

`data.data_source`를 화면 상단 또는 하단에 표시한다.

문구 예:

```text
데이터 출처: 공공데이터포털 한국환경공단 전기차 충전소 정보
```

mock이면:

```text
테스트용 mock 데이터
```

2. 상태 갱신 문구를 수정한다.

현재 "10초마다 갱신"은 괜찮지만, "실시간 충전현황"은 조금 강하다.

권장:

```text
공공데이터 기준 충전기 상태
```

3. 충전기 선택 후 계산 화면의 한계를 표시한다.

상세 화면에서 충전기를 누르면 충전 계산으로 이동한다. 이 계산은 단순 출력 기준이므로 안내를 추가한다.

```text
충전기를 선택하면 단순 출력 기준의 예상 금액·시간을 계산합니다.
```

---

## 2.12 충전 계산 화면

파일:

- `frontend/src/app/charge/page.tsx`
- `frontend/src/lib/charge.ts`

### 보완사항

1. 단순 계산임을 표시한다.

현재 계산식:

```text
예상 소요시간 = 추가 충전량(kWh) / 충전기 출력(kW) * 60
```

실제 전기차 충전은 고SoC 구간에서 느려지므로, 화면에 다음 문구를 추가한다.

```text
단순 정출력 기준 계산입니다. 실제 충전 시간은 배터리 온도와 충전 상태에 따라 달라질 수 있습니다.
```

2. 목표 충전량 제한을 둔다.

목표값이 100%를 넘거나 음수가 되는 경우를 프론트에서 막는다.

3. 요금 출처를 표시한다.

단가가 공공 API 원천값이 아니면:

```text
단가는 앱 내 참고값입니다.
```

---

## 2.13 로그인 화면과 브랜드

파일:

- `frontend/src/app/login/page.tsx`
- `bydLogo.png`
- `frontend/public/bydLogo.png`

### 보완사항

1. "로그인"을 "시작하기"로 변경한다.

현재 인증이 없으므로 로그인이라는 표현은 오해를 만든다.

2. BYD 로고 사용을 개인 테스트 범위로 제한한다.

개인 로컬 테스트에서는 큰 문제가 되지 않을 수 있지만, GitHub 공개·시연·배포 시에는 공식 앱처럼 보일 수 있다.

공개 가능성이 있으면 다음 중 하나를 선택한다.

- BYD 로고 제거
- 자체 아이콘 사용
- "비공식 테스트 앱" 문구 표시

---

## 3. 개발 중 점검 명령

### 프론트 빌드

```bash
cd frontend
npm run build
```

### 프론트 취약점 확인

```bash
cd frontend
npm audit --omit=dev
```

현재 점검 시점에는 high 등급 취약점이 발견되었으므로, 외부 공개 전 의존성 업데이트와 재점검이 필요하다.

### 백엔드 문법 확인

```bash
cd backend
python -m compileall app
```

### 키 노출 확인

```bash
rg -n "KAKAO|API_KEY|SERVICE_KEY|SECRET|TOKEN|Authorization" -g "!frontend/node_modules/**"
```

---

## 4. 개인 테스트 운영 수칙

- 앱은 로컬에서만 실행한다.
- `.env`, `.env.local`은 공유하지 않는다.
- 실제 주소가 들어간 화면 캡처를 공유하지 않는다.
- 카카오 Developers의 허용 도메인을 최소화한다.
- mock 데이터와 실데이터를 구분해서 본다.
- 추천 충전소는 실제 방문 전 지도 앱, 충전 사업자 앱, 현장 안내로 재확인한다.
- 장거리 실주행 테스트 전에는 여유 배터리와 대체 충전소를 별도로 확보한다.

---

## 5. 추천 적용 순서

1. `main/page.tsx`의 개인 주소 바로가기 제거
2. README와 `charging.py`의 주행거리 기준 통일
3. mock/실데이터 출처 UI 표시
4. "실시간" 문구 완화
5. 지오코딩 실패 시 서울 폴백 제거
6. `sessionStorage` 만료 또는 전체 초기화 강화
7. 충전 계산 화면에 단순 계산 고지 추가
8. `npm audit` 취약점 업데이트
9. 주요 경로 테스트 케이스 추가

